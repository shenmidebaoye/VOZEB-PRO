"use client";

/** Community inspiration gallery removed in local studio mode. */
export function CreateInspirationGallery(_props: { onUsePrompt: (prompt: string) => void; onUseImage: (item: never) => Promise<void> }) {
    return null;
}
